using System;

public class Solution {
    public int solution(int slice, int n) {
        int answer = 0;
        
        do
        {   
            answer ++;
        }
        while(n > slice*answer);
        
        return answer;
    }
}